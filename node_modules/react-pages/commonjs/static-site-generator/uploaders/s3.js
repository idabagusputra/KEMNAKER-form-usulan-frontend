"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = S3Uploader;
var _s = _interopRequireDefault(require("@auth0/s3"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
function S3Uploader(_ref) {
  var accessKeyId = _ref.accessKeyId,
    secretAccessKey = _ref.secretAccessKey,
    region = _ref.region,
    bucket = _ref.bucket,
    ACL = _ref.ACL;
  return function upload(directory, _ref2) {
    var started = _ref2.started,
      progress = _ref2.progress;
    var params = {
      localDir: directory,
      deleteRemoved: true,
      // default false, whether to remove s3 objects
      s3Params: {
        ACL: ACL,
        Bucket: bucket
      }
    };
    var s3Client = _s["default"].createClient({
      maxAsyncS3: 20,
      // this is the default
      s3RetryCount: 3,
      // this is the default
      s3RetryDelay: 1000,
      // this is the default
      multipartUploadThreshold: 20971520,
      // this is the default (20 MB)
      multipartUploadSize: 15728640,
      // this is the default (15 MB)
      s3Options: {
        accessKeyId: accessKeyId,
        secretAccessKey: secretAccessKey,
        region: region
      }
    });
    console.log();
    console.log('(the S3 upload progress bar seems to be buggy, just wait until it exits)');
    console.log();
    var uploader = s3Client.uploadDir(params);
    var initialized = false;
    var finished = false;
    uploader.on('progress', function () {
      if (!initialized && uploader.progressMd5Total) {
        started(uploader.progressMd5Total);
        initialized = true;
      }
      if (initialized) {
        var progressSoFar = uploader.progressMd5Amount / uploader.progressMd5Total;

        // `s3` spams for a lot of `progress` with `1`.
        if (progressSoFar === 1) {
          if (!finished) {
            finished = true;
            progress(1);
          }
        } else progress(progressSoFar);
      }
    });
    return new Promise(function (resolve, reject) {
      uploader.on('end', resolve);
      uploader.on('error', reject);
    });
  };
}
//# sourceMappingURL=s3.js.map